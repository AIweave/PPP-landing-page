body {
    background-color: #c2def9;
    font-size: 100%; /*using body % em (ex. 2.5em for h1 {font-size} is good for all browswers, 
    allows zoom or resize of text, and shows the same text size in all browsers*/
    overflow: hidden; /*removes horizontal & vertical scroll bar*/
}
nav {
    background-color: #003B6D;
    text-align: center;
    justify-content: center;
    overflow: auto;
    width: 100%;
    display: flex; /*makes a flex container*/
}
#doglogo, #plogo{
    background-color: #c2def9;
    border:none;
}
#doglogo2, #plogo2{
    background-color: #ccc;
    border: none;
}
a {
    color: white;
    text-decoration: none; /*removes underline*/
    border-right: 1pt solid white;
    padding: 8px; /*for clickable space*/
    font-size: 17px;
    width: 33.33%; /* Four links of equal widths (100% bar split 4*/
    background-color: #003B6D;
    text-align: center;
}
a:last-child{
    border-right: none; /*removes border from the last item (anchor)*/
}
a:hover{
    background-color: #676767;
}
/*a:active{
    background-color: #BDBDBD;
}*/
* {box-sizing: border-box;}

.searchbox {
    float: right;
    position: relative;
    top: -86px;
  }
  
div input[type=text] { /* modifies SEARCH BOX from default settings*/
    padding: 8px;
    margin-top: 8px; /*space directly after the box itself; space between box and menu*/
    font-size: 17px;
    border: none;
    
  }
  
div #searchbutton {
    display: flex;
    float: right;
    padding: 9px 10px;
    margin-top: 8px;
    /*margin-right: 16px; the higher the value, the further left*/
    background: #ddd;
    font-size: 17px;
    border: none;
    cursor: pointer; /*changes cursor to little hand when over icon*/
  }
  
div .searchboxbox{
    background: #ccc;
  }

div #searchbutton:hover{ /*must call button to configure*/
    background-color: #ccc;
    
}
#doglinks{
    background-color:#c2def9;
    color: #003B6D;
    background-color: transparent; /*remmoves background while keeping content (bkgrd remover*/
    padding:0; /*removes space outside of content (text in this case) so it can flow & look better*/
}
#doglinks:hover{
    color:white;
}
#dogs{
    color: #003B6D;
    text-align: center;
}
#dogtitle{
    position: relative;
    left: 50px;
    font-size:2.5rem;
}
#dogs p{
    text-align: left;
    color: black;
}
#dogs img{
    border-radius: 100px; /*100 = 100% round, 50=50% round, 0=original content shape (probably square)*/
    box-shadow: 5px 10px 18px #888888;
    box-sizing: border-box; /*include padding & border in an element's total width and height (preferred)*/
    width: 150px;
    height: 150px;
    float: right;
    position:relative;
}
footer{
    background-color: #ccc;
    color: #003B6D;
    display: block;
    position:fixed;
    bottom: 0; /*bottom of screen*/
    left: 0; /*left end of screen*/
    width:100%;
}
#subscribe{
    display: inline; 
    position:relative; 
    left: 270px;
    top: -1px;
    border-radius: 4px;
    width: 20%;
}
#subscribebutton{
    position: relative;
    display: inline;
    font-size: 16px;
    left: 290px;
    background-color: #003B6D;
    border-radius: 8px;
    color: white;
    cursor: pointer;
}
footer p{
    display: inline;
    position: relative;
    left: 950px;
    top: -1px;
    color: #003B6D;
}
